[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so client-side routing (locale-prefixed public routes + /admin/*) works on refresh/direct load.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
