# External Blockers

Items that require a secret, account, or domain the build agent does not have. Each has a dev-safe fallback in place so the rest of the project is unaffected.

| ID | Item | Fallback in place | Needed from user before real production launch |
|---|---|---|---|
| BLK-001 | Production PostgreSQL connection string (Railway) | Local/dev connection string via `.env` / `appsettings.Development.json`; `DATABASE_URL` env var read at startup | Railway project + `DATABASE_URL` |
| BLK-002 | JWT signing secret for production | Dev-only secret generated locally, never committed; `Jwt__Secret` env var expected | Strong secret set in Railway env vars |
| BLK-003 | Netlify site + production API URL | Frontend reads `VITE_API_BASE_URL` from env, defaults to `http://localhost:5091/api/v1` in dev (matches `backend/Api/Properties/launchSettings.json`) | Netlify site, env var pointing at deployed backend |
| BLK-004 | CAPTCHA provider (reCAPTCHA/hCaptcha) key for Travel Request anti-spam | Honeypot field + IP rate limiting (see DEC-004) | Provider account + site/secret key if stronger anti-spam desired |
| BLK-005 | Transactional email provider (e.g. for notifying staff of new Travel Requests) | **Update (2026-09-03, see DEC-016):** Code-complete — `SmtpEmailService` (`backend/Infrastructure/Email/SmtpEmailService.cs`) sends a new-Travel-Request notification via generic SMTP (works with a Gmail app password, no provider SDK needed), selected automatically whenever `Email:Smtp:Host`/`User`/`Password` are configured; `NullEmailService` is the fallback otherwise (logs a warning, never throws — request creation always succeeds even if email is unset or the send fails). Not yet verified against a real mailbox — no SMTP credentials available in the build environment. | SMTP credentials as env vars: `Email__Smtp__Host`, `Email__Smtp__Port` (587 for Gmail), `Email__Smtp__User`, `Email__Smtp__Password` (a Gmail **app password**, not the account password, if using Gmail), optionally `Email__Smtp__From` (defaults to `User`); confirm/override the notification recipient via `Email__NotifyTo` (defaults to `duneisipehr@gmail.com`, set in `appsettings.json`) |
| BLK-006 | Custom production domain | Railway/Netlify default subdomains used in deployment docs | Domain registrar + DNS access |
| BLK-007 | Real destination/offer photography | Placeholder image URLs (picsum/local static) used in dev seed data only, never in production code paths | Licensed photography or stock subscription |
| BLK-008 | Protected object storage for uploaded passport photos (sensitive PII, see DEC-012) | Code-complete: `R2FileStorageService` (Cloudflare R2, S3-compatible) implements `IFileStorageService` and is selected automatically whenever `Storage:R2` credentials are configured; `LocalFileStorageService` remains the dev-only fallback (`backend/Api/uploads/`, gitignored). **Update (2026-09-01):** with R2 still unconfigured, production passport-photo upload was 500ing on every attempt (confirmed live 2026-08-28), which actually broke the public `/travel-request` form (client-side validation requires at least one successfully uploaded photo before submit is enabled) — not just a missing-preview gap. Added a **temporary third fallback**, `DbFileStorageService` (`backend/Infrastructure/Files/DbFileStorageService.cs`), storing photo bytes directly in Postgres via a new `PassportPhotos` table (`bytea` content + `ContentType`, keyed by the same `{guid}{extension}` filename convention Local/R2 use — `TravelRequest.PassportPhotoPaths` and the existing admin passport-photo endpoint needed zero changes). `DependencyInjection.AddInfrastructure` now picks, in order: R2 if credentials are configured (any environment) → Local if `Development` and no R2 credentials → **Db bytea fallback otherwise**. This is explicitly **not** a permanent architecture — Postgres isn't sized or built for BLOB storage. **When real R2 credentials arrive:** existing rows in `PassportPhotos` need a one-off migration script to upload their bytes into the R2 bucket (keyed by their existing `FileName`, so `TravelRequest.PassportPhotoPaths` values stay valid unchanged) before/alongside setting the `Storage__R2__*` env vars — otherwise requests submitted during the bytea-fallback window will silently 404 on photo view once R2 takes over, since R2 won't have those keys. | An actual Cloudflare R2 bucket (**private**, no public-read policy) plus its Access Key/Secret Key/endpoint, set as `Storage__R2__AccessKey`/`Storage__R2__SecretKey`/`Storage__R2__BucketName`/`Storage__R2__Endpoint` env vars on the production host, **plus** the bytea→R2 migration step described above for any photos uploaded during the fallback window |

None of the above block Stages 0–23 of local development; all are documented here per policy and re-checked in Stage 22 (Production Preparation) and Stage 23 (Final Audit).
